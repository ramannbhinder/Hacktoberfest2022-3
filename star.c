#include<stdio.h>
int main()
{
    int i,j,rows,columns, a[5][5], b[5][5],count=1;
    printf("Enter the number of rows and columns : \n");
    scanf("%d %d", &i,&j);
    
    printf("Enter the elements\n");
    for(rows=0;rows <i; rows++)
    {
         for(columns=0;columns<j;columns++)
         {
            for(columns=0;columns<j;columns++)
            {
                scanf("%d", &a[rows][columns]);
            }
         }
         for()

    }
}
